# ############################################################################
# Model: Whisper Encoder-Decoder
# Augmentation: SpecAugment
# Authors: Luca Della Libera 2022
# ############################################################################

# Seed needs to be set at top of yaml, before objects with parameters are made
seed: 1235
__set_seed: !apply:torch.manual_seed [!ref <seed>]

# Data preparation
dataset_size: medium
dataset_version: "10_0"
dataset_dir: !ref ../data/common_voice_<dataset_version>
manifest_dir: !ref <dataset_dir>_<dataset_size>
locales: null # All languages

# Data files and directories
output_dir: !ref results/multilingual/<dataset_size>/whisper_large_encoder_decoder_test/<seed>
wer_file: !ref <output_dir>/wer.txt
save_dir: !ref <output_dir>/save
train_log: !ref <output_dir>/train_log.txt

# We remove utterances longer than 10s in the train/dev/test sets as
# longer sentences certainly correspond to "open microphones".
avoid_if_longer_than: 10.0

ckpt_interval_minutes: 15 # save checkpoint every N min

# Training parameters
number_of_epochs: 0
lr_model: 0.0001
sorting: ascending
auto_mix_prec: True

# With data_parallel batch_size is split into N jobs
# With DDP batch_size is multiplied by N jobs
# Must be 8 per GPU to fit 32GB of VRAM
batch_size: 3
test_batch_size: 16

# Feature parameters
sample_rate: 16000

# Dataloader options
train_dataloader_opts:
    batch_size: !ref <batch_size>
    shuffle: True
    num_workers: 6

valid_dataloader_opts:
    batch_size: !ref <test_batch_size>
    num_workers: 6

test_dataloader_opts:
    batch_size: !ref <test_batch_size>
    num_workers: 6

# Model parameters
whisper_hub: openai/whisper-large
freeze_encoder: True

# Decoding parameters
max_gen_tokens: 200
forced_decoder_locale: null

# Time domain SpecAugment
augmentation: !new:speechbrain.lobes.augment.TimeDomainSpecAugment
    sample_rate: !ref <sample_rate>
    speeds: [95, 100, 105]

# Tokenizer
tokenizer: !name:transformers.WhisperTokenizer.from_pretrained
   pretrained_model_name_or_path: !ref <whisper_hub>
   language: null # Must be set dynamically for each sample before encoding based on the locale
   task: transcribe
   predict_timestamps: False

# Models
whisper: !new:speechbrain.lobes.models.huggingface_whisper.HuggingFaceWhisper
   source: !ref <whisper_hub>
   freeze_encoder: !ref <freeze_encoder>
   save_path: !ref <save_dir>/whisper_checkpoint
   encoder_only: False
   freeze: True

log_softmax: !new:speechbrain.nnet.activations.Softmax
    apply_log: True

nll_loss: !name:speechbrain.nnet.losses.nll_loss

modules:
    whisper: !ref <whisper>

model: !new:torch.nn.ModuleList
    - [!ref <whisper>]

# Optimizers
opt_class: !name:torch.optim.Adadelta
    lr: !ref <lr_model>
    rho: 0.95
    eps: 1.e-8

lr_annealing: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr_model>
    improvement_threshold: 0.0025
    annealing_factor: 0.8
    patient: 0

# Metrics
error_rate_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats

cer_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats
    split_tokens: True

english_normalizer: !name:transformers.models.whisper.english_normalizer.EnglishTextNormalizer

# Misc
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_dir>
    recoverables:
        model: !ref <model>
        scheduler: !ref <lr_annealing>
        counter: !ref <epoch_counter>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>
